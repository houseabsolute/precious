# how to apply include/exclude rules on_dir when filter just gets the dir?

Make the basepaths return some sort of more complex struct that includes a dir
and the files in that dir. Then we can apply rules to files but pass dirs to
filters.

# git stage & unstage for -g flag

# tests & CI

# code cleanup like move parallel execution to shared sub

Hard to figure out parallel closure issues with type checking.

# docs

how to exclude all but one file from a dir tree (2 filters, one excludes, the
other includes just the relevant files).
